# Project Log — Modi Ne Kiya Kya Hai

**Read this file first in any new session on this project.** It's the persistent memory the user explicitly asked for — what's done, what's broken, what's next — so context survives across sessions instead of resetting every time.

Live site: https://modinekiyakyahai.com (also .vercel.app, www.). Repo: https://github.com/authenticTyagi/namo. Admin: https://modinekiyakyahai.com/admin (Google sign-in, `ADMIN_EMAILS` env var gates access).

## What this project is

A sourced, fact-checked, bilingual (Hindi/English) reference site documenting verifiable government work under PM Modi's leadership. Core commitment, non-negotiable: **every claim is cited, nothing is published without a source, mixed/negative data is reported honestly (not spun)**. Explicitly NOT a propaganda site, NOT affiliated with any government body or political party — see `/methodology` and `/about`. No individual is named anywhere on the site (deliberate privacy choice by the owner).

## Stack

Next.js 15.5.25 (pinned stable — npm's `latest` tag currently resolves to a 16 canary, don't let create-next-app grab it again), Tailwind v4, next-intl (5 locales: hi default, en, bn, te, mr), Drizzle ORM + Neon Postgres, NextAuth v5 + Google OAuth, Vercel hosting with GitHub auto-deploy connected (push to `master` → deploys automatically, no manual `vercel --prod` needed anymore).

## Current state (as of 2026-09-15)

- **10 categories, 34 entries, all published** (2026-09-15): added Agriculture & Farmer Welfare, Environment & Renewable Energy, Education & Skill Development, and Health Infrastructure (3 entries each, via 4 parallel research subagents across the session) — every new category's entries are deliberately `impactType: "mixed"` with real, specific honest caveats, not just wins. PLI scheme manufacturing growth and INS Vikrant (both drafted by the local cron job) have also been approved and are live. Full breakdown: Economy-Infra-Digital (9), Space & Science (3), Foreign Policy & Diplomacy (3), Defense & Security (3), Currency/Prices/Inflation (2), Welfare & Social Schemes (4), Agriculture & Farmer Welfare (3), Environment & Renewable Energy (3), Education & Skill Development (3: PMKVY, DIKSHA/PM eVidya, higher-ed GER), Health Infrastructure (3: AIIMS, Jan Aushadhi, medical seats) — all cited, all with before/after stat comparisons. Categories 9 and 10 deliberately do NOT get an invented accent color (the validated 8-hue dataviz palette is fully used) — they share a documented neutral gray instead (`src/lib/category-colors.ts`).
- **20 editorials total, all pending_review** (2026-09-15, second batch): 10 more written in the historically-grounded voice (`EDITORIAL_VOICE.md`), covering entries that had none — spans economy, currency, welfare, space, foreign-policy, agriculture, and environment. Need approval from `/admin/editorials`.
- **4 "India in the World" comparisons total, all pending_review** (2026-09-15): poverty (MPI vs. Bangladesh/Pakistan/Nigeria/world), renewable generation share (India ranks **last** of 6 — a genuinely unflattering, honestly-reported finding), global arms-export share (India doesn't make SIPRI's top-25 exporters list, though it's the world's 2nd-largest *importer* — also honestly reported), and life expectancy (India trails China/Bangladesh/South Asia average/world average, leads only Pakistan). Need approval from `/admin/comparisons`.
- **Declined topics, with reasoning given to the user** (2026-09-15): Kashmir, POK, "wars and border disputes," and general/cross-border terrorism — these require taking a position on active, contested geopolitical disputes or attributing acts to specific foreign actors, which conflicts with scope notes already written into `foreign-policy-diplomacy` ("does not characterize other countries") and `defense-security` ("not military outcomes or specific conflicts"). **Naxalism/Left-Wing Extremism was NOT declined** — agreed as an in-scope narrow security-statistics topic (LWE-affected district count trend, violence incidents), same treatment as any other defense/security trade-and-production figure, no individual naming, not yet researched.
- **Unified admin review queue** (2026-09-15): `/admin/review` now shows entries + editorials + comparisons pending review together, one page, inline approve/reject for all three (`UnifiedReviewActions.tsx` dispatches to the existing per-type server actions — no new mutation logic). `/admin/editorials` and `/admin/comparisons` are now pure list/management views (their own pending-review sections removed). `AdminNav` shows live count badges (combined pending count next to "Review queue", flagged-comment count next to "Comments"), fetched once in the shared protected layout. Dashboard collapsed 3 "pending X" tiles into 1 "Needs review" tile.
- **Micro/macro lens — new standing content requirement** (2026-09-15): user asked that every entry, editorial, and comparison going forward explicitly address (a) **micro** — what changed in an ordinary person's/household's/small business's actual day-to-day life, and (b) **macro** — what changed for India's standing, capability, or autonomy in the world (self-reliance, agenda-setting capacity, reputation). Documented in new `scripts/entries/MICRO_MACRO_LENS.md`, which explicitly draws the same boundary already established for editorial voice and declined topics: "macro/sovereignty" here means **capability and autonomy** (Atmanirbhar Bharat, defense/vaccine self-reliance, G20 agenda-setting, diplomatic reach), never territorial claims or characterizing a foreign country adversarially — Kashmir/POK/wars/terrorism stay declined. Neither lens overrides the honesty rule; a genuinely thin macro angle or a genuinely mixed micro effect gets reported as such, not manufactured otherwise. Applies **going forward only** — the 34 existing entries, 20 editorials, and 4 comparisons already written were not retroactively rewritten; that would be a separate explicit decision if the user wants it done. Not yet wired into the research-subagent prompts used for new content batches — do that the next time a content-expansion round is run.
- **Sitemap, robots.txt, homepage share, look-and-feel pass, AdSense ID update** (2026-09-15): `/sitemap.xml` (dynamic, every published entry/editorial/comparison/category × all 5 locales, with hreflang alternates) and `/robots.txt` — the site had neither before this. Homepage now has its own site-level share button (previously only per-page). Header made sticky+blurred, nav moved to `lg` breakpoint to fit 8 links (MobileMenu's breakpoint synced to match), shared `CARD_CLASS` (`src/lib/utils.ts`) adds a hover lift+shadow across every card grid, homepage hero got a soft brand-tinted background, Footer got more presence (name/tagline/About link). AdSense verification meta tag updated to a new publisher ID (`ca-pub-1804566337195012`, replacing `ca-pub-6506096146746148`) at the user's request — not yet confirmed which AdSense account this belongs to or whether ad units are live; same "code alone doesn't turn ads on" caveat as before.
- **Backlog clear-out round 3: COVID-19, E20 ethanol, recession comparison — backlog now cleared** (2026-09-15): `covid19-domestic-vaccination-pandemic-response` (health-infrastructure, impactType mixed, the most sensitive entry on the site) — the domestic vaccination drive (2.2B+ doses, Covaxin/Covishield, CoWIN) reported alongside the April-June 2021 second-wave crisis (oxygen/bed/ICU shortages, Supreme Court oxygen task force) in full, and the documented, unresolved gap between India's official death toll (533,849) and independent excess-mortality estimates (WHO ~4.7M; Center for Global Development 3.4-4.9M) — reported as a disputed, unresolved fact with no blame assigned either direction. **Given the sensitivity, this one wasn't just wired in on trust**: independently re-verified the load-bearing numbers myself via direct WebFetch (WHO release, CGD paper, Wikipedia) after the drafting agent flagged its own WebSearch budget had run out mid-task — confirmed the official toll, the WHO figure, the CGD range, and the April 30 case/death numbers all check out; tightened one citation note where the "government disputed WHO's methodology" claim wasn't cleanly pinned to a source. `e20-ethanol-blending` (environment-renewable-energy, mixed: 20% blending hit 5 years early with real forex/emissions claims, but honest on the food-vs-fuel grain-feedstock debate and real mileage loss on older vehicles). `india's-gdp-in-the-2020-global-recession` comparison (economy-infra-digital, sourced only to World Bank/IMF per the comparisons rule — genuinely counter-narrative finding: India's 2020 GDP contraction (-5.8%) was *steeper* than the US, Germany, Japan, and Brazil, only rebounding faster in 2021-22 — the "fastest-growing major economy" framing doesn't hold for the crisis year itself, reported plainly). All landed `pending_review` (seed.ts fix holding correctly across every round). This closes the backlog list from the topics batch (Naxalism/LWE, FDI, IT/AI, Women & Child Development, BRICS, per-capita income, tax base, E20, COVID-19, recession) — 12 new entries + 1 new category + 1 comparison across 3 rounds of parallel research subagents, all pending admin review at `/admin/review`.
- **Backlog clear-out round 2: FDI, IT/AI, per-capita income, tax base** (2026-09-15): 4 more entries into the existing `economy-infra-digital` category, all correctly landed `pending_review` (confirming the seed.ts fix above works). `fdi-record-inflows-net-fdi-decline` (mixed: gross FDI hit a record $81.04B in FY24-25, but RBI's own net-FDI figure — after repatriation/outward investment — crashed 96% to just $0.4B). `it-services-growth-and-ai-compute-push` (mixed: tech exports nearly tripled to ~$246B and IndiaAI Mission GPU access scaled to 38,000+, but India's AI compute/chip base still trails the US/China by a wide margin, first fab still under construction). `per-capita-income-real-growth-global-rank` (mixed: real per-capita NNI up 57% in a decade, but India's global per-capita GDP rank barely moved — ~153rd of 195 nominal — and the top 10% captured 57.7% of the gain; deliberately scoped apart from the existing `india-fourth-largest-economy` entry). `direct-tax-base-and-collection-growth` (mixed: direct tax collections more than tripled and ITR filers more than doubled, but the filer base is still only 6.68% of the population and ~70% of FY22-23 filers owed zero tax due to rebates). Need approval from `/admin/review`.
- **Backlog clear-out round 1: Women & Child Development category + Naxalism/LWE + BRICS, and a real seed.ts bug fixed** (2026-09-15): new 11th category "Women & Child Development" (`women-child-development`, neutral-gray per the documented 9th+ treatment) with 3 entries — Beti Bachao Beti Padhao sex-ratio-at-birth trend (mixed: HMIS shows 918→930 but independent CRS data shows a rockier 877-in-2016 dip and several states backsliding since 2020, plus CAG/parliamentary-committee fund-utilisation findings), Poshan Abhiyaan child stunting (mixed: real decline but at under half the mission's own annual target pace, India still 102nd/123 on the 2025 Global Hunger Index), and female labour force participation (mixed: FLFPR nearly doubled to 41.7% but researchers flag most of the gain as unpaid family labour/self-employment, not paid work, only ~9.3% in a regular wage job). Plus 2 standalone entries slotted into existing categories: `lwe-affected-districts-decline` (defense-security — LWE-affected districts 126→38→3 "most affected," narrowly scoped to MHA administrative statistics, no individual named, honestly notes the government's "fully eradicated" claim is contested by continued documented activity) and `brics-2026-chairship-ndb-lending` (foreign-policy-diplomacy — 2026 BRICS chairship/NDB lending, honestly correcting the popular "de-dollarization" framing since India's own government has repeatedly said there's no BRICS-currency proposal). **Bug found and fixed**: `scripts/seed.ts` was inserting every new entry with `status: "published"` directly — an inconsistency with editorials/comparisons/the pipeline API, which all correctly land as `pending_review`. This had been the case for every content-pack round all session (agriculture, environment, education, health-infra all went straight to published this same way) — not touching those retroactively, but fixed going forward and reverted these 5 brand-new entries back to `pending_review` before they'd been seen. Need approval from `/admin/review`.
- **Trusted sources whitelist, category counts/nav/dates, unpublish for editorials/comparisons, homepage stats, stale-copy fixes** (2026-09-15): `/admin/trusted-sources` — standing admin-curated whitelist (handle/URL, platform, credibility tier, video-source flag), distinct from `/admin/sources`' one-off URL-suggestion inbox; pipeline endpoint `/api/pipeline/trusted-sources` for research agents to check a citation's publisher against it. Homepage/`/categories`/category-detail pages now use the previously-unused `category-colors.ts`/`category-icons.ts` (icon + accent color + published-entry count on every category card); new `/categories` index page + nav link for scalability as the category list grows. Publish dates now render on entries and editorials (data already existed in the query layer, just never rendered). Editorials and comparisons can now be **unpublished** (`draft` added to `editorialStatusEnum`, additive; comparisons already had it via `entryStatusEnum` reuse) — mirrors entries' existing publish/unpublish toggle exactly, for pulling something already live without marking it "rejected" or deleting it. Homepage hero now shows a live stat strip (published entries/categories/editorials/sources cited, via new `getPublicSiteStats()`). `/overview` gets a jump-to-category nav past 3 categories. Fixed stale content: About/Privacy said "Hindi and English" (now lists all 5 locales), Terms called comments "(future)" when they've been live, and **Methodology's "not comparison" claim directly contradicted the India-in-the-World feature** — reworded to explain comparisons use only neutral international sources, added a paragraph on editorials' looser sourcing bar, and reworded the scope boundary to explain what's excluded (territorial disputes, active conflicts, attributing violence to a country/group) rather than claiming no comparisons happen.
- **"Fourth Largest Economy" editorial rewritten with real per-capita numbers, reset to pending_review** (2026-09-15): user felt the published editorial stated "per-capita income remains a small fraction" without actual numbers, and separately proposed that low income-tax-filer numbers mean that portion of the population "isn't contributing" and "is just taking" subsidies. Agreed with the first point, declined the second: added India's nominal per-capita GDP (~$2,700-2,800, ~150th of ~195 economies, IMF WEO Apr 2026) and PPP per-capita (~$12,800, ~127th globally), plus the ITR-filer base fact (8.09 crore filers, 6.68% of population, Ministry of Finance reply to Lok Sabha, FY23-24) — but explained why "just taking, not contributing" goes past what the data supports (most non-filers are below the tax-free threshold because average income is genuinely low, which the per-capita numbers already establish; they still pay GST and are the labor behind the GDP number) and left that framing out. Formalized as a general rule in `MICRO_MACRO_LENS.md` ("Aggregate numbers need their per-capita partner"): any aggregate figure (GDP, reserves, defense budget, exports) must be paired with its per-capita/normalized equivalent and source in the same post, going forward. This editorial was **published** when the rewrite happened — content updated AND status reset to `pending_review` in the same operation (not left live), consistent with the process fix from the earlier UPI-editorial incident. Needs fresh approval from `/admin/editorials`.
- **10 editorials hand-authored, then rewritten in a new voice** (2026-09-15): one per existing entry across 6 categories, honestly toned (5 positive, 5 mixed — none forced positive). User approved and published all 10 from `/admin/editorials`, then asked for a "nationalistic" editorial persona centered on Sanatan/civilizational history, "anti-Hindu sentiment," and blaming political eras/parties — **declined that specific framing** (conflicts with this project's own founding "not propaganda, not any party" commitment, and asks the site to take a side on contested historical/communal narratives its sourced-citation method can't adjudicate) and proposed an alternative instead: grounding editorials in colonial-era extraction, license-raj scarcity, and the real difficulty of nation-building, with India's ancient heritage referenced as pride, never grievance. User approved the alternative. See `scripts/entries/EDITORIAL_VOICE.md` — the full persona spec and hard boundaries, wired into the local editorial cron job's prompt. All 10 editorials were rewritten in this voice and reset to `pending_review` (one of them had already been re-published mid-rewrite by mistake — caught, explained to the user, and reverted). **Still need fresh admin approval** from `/admin/editorials` — nothing from this rewrite is live.
- **Local DB connectivity issue discovered (2026-09-15, unresolved)**: mid-session, this machine's Node processes stopped being able to reach the Neon database directly — both `drizzle-orm/neon-http` (fetch-based) and `drizzle-orm/neon-serverless` (websocket-based) time out connecting to the compute endpoint (`ep-polished-glitter-ayglotlk.c-5.us-east-2.aws.neon.tech`), while `curl` reaches the same host fine and the live Vercel deployment reaches the DB fine (confirmed: `npm run db:seed`/migrations worked earlier this same session). Looks like an app-level block on `node.exe`'s outbound connections to that specific host (Windows Firewall/AV are the likely suspects — not confirmed, no admin access from this tool to check). **Workaround**: added `/api/pipeline/editorial-update` (CRON_SECRET-authed) so one-off content fixes can go through the already-working Vercel→Neon path via HTTP instead of a direct local DB connection. If `npm run db:seed`, `drizzle-kit migrate`, or any local script suddenly can't reach the DB in a future session, check this first before assuming something is broken in the code.
- **Vercel Analytics added** (2026-09-15): `@vercel/analytics` wired into the root layout. Chosen over a public visitor counter (looks bad early, easy to misread, no cookie-consent complexity). **Needs enabling in the Vercel project dashboard's Analytics tab** to actually start showing data — the code alone doesn't turn it on; ask the user to confirm they've done this.
- **Admin panel** (`/admin`): dashboard, entries list (publish/unpublish), review queue (empty, ready for pipeline output), sources intake, feedback inbox — all working, Google-auth-gated
- **Public feedback form** (`/feedback`): replaces the old broken mailto link
- **Automation pipeline API** (`/api/pipeline/ingest`, `/api/pipeline/existing-slugs`): built, tested, verified working end-to-end against production. Server-enforced sourcing bar (≥1 official_primary source OR ≥2 total), server-owned confidence scoring, always lands as `pending_review`
- **Overview page** (`/overview`): every stat from every entry in one place, category-colored accents, scroll-reveal
- Mobile nav (hamburger menu), Facebook share button, cookie-consent banner (for future AdSense) all shipped
- **5-locale architecture** (2026-09-15): site now routes `/hi /en /bn /te /mr`. bn/te/mr fall back to English (then Hindi) for entry/category content until hand-authored — that's a deliberate separate follow-up pass, not done yet. UI chrome (nav, buttons, form labels) IS hand-translated into all 3 new languages already (`messages/{bn,te,mr}.json`). `LanguagePicker.tsx` (replaced the old hi/en `LanguageToggle`) shows all 5 in a dropdown, native names, so the picker itself doesn't gate on content being ready. Legal/policy pages (About/Methodology/Terms/Privacy) stay hi/en-only by design — need owner review before another language. See `src/lib/localized.ts` for the resolution/fallback logic and `src/db/schema.ts`'s `entryTranslations`/`entryStatTranslations` tables (empty, additive, ready for the future content pass) plus `categories`/`tags`' new `nameBn/Te/Mr` etc. columns.
- **AdSense verification meta tag** (2026-09-15): `ca-pub-6506096146746148` added via the `[locale]` layout's `metadata.other` — publisher ID provided, but ad units/slots themselves aren't built yet (waiting on account approval).
- **Editorials + comments (Phase 2)** (2026-09-15): `/editorials` and `/editorial/[slug]` — opinion pieces about a specific already-published entry (`relatedEntryId`), tone is honest (positive/negative/neutral/mixed, never forced positive), looser sourcing bar than entries (no per-claim citation requirement — anchored by the entry it's about instead). Comments live under both editorials AND entries now (`src/components/comments/`) — Google sign-in required to post (a public, non-admin entry point into the existing NextAuth flow, `SignInToComment.tsx`), report button feeds `/admin/comments` moderation queue. `/admin/editorials` review queue + `/admin/editorials/new` manual-creation form (needed because the editorial drafting agent has no working automation yet — see issue #2 below, same root cause). Pipeline API exists (`/api/pipeline/editorial-ingest`, `/api/pipeline/editorial-candidates`) and is tested end-to-end, but nothing calls it on a schedule yet.
- **"India in the World" — cross-country comparisons** (2026-09-15): `/india-in-the-world` (index) and `/india-in-the-world/[slug]` (detail) — India's own figure on a metric set beside 4-6 other countries in a ranked bar chart (`ComparisonBarChart`, India highlighted in the brand accent), sourced ONLY to neutral international bodies (World Bank, IMF, IEA/IRENA, UNDP, UN agencies) — deliberately not Indian government sources, so a reader can't wave off the comparison as self-reported. Same honesty rule as the rest of the site: reports it plainly when India trails a peer. New tables `comparisons`/`comparison_points`/`comparison_sources` (additive, migration 0006). Optionally anchored to an existing entry via `relatedEntryId` (nullable — can also stand alone, unlike editorials). `/admin/comparisons` review queue + `/admin/comparisons/new` manual form (no drafting pipeline built for this yet, matching editorials' initial state). **Initial content**: 2 comparisons requested from research subagents — renewable electricity generation share vs. G20 peers, and extreme-poverty-rate reduction vs. regional peers — check `/admin/comparisons` for whether these landed as pending_review by the time you read this.

## Known issues / open items

1. ~~Admin login blocked on the custom domain~~ **FIXED 2026-09-15**: user added `https://modinekiyakyahai.com/api/auth/callback/google` and the `www.` variant as authorized redirect URIs in Google Cloud Console. Verified end-to-end (both apex and `www.`) by simulating the actual NextAuth sign-in flow via curl — CSRF token → POST `/api/auth/signin/google` → followed the real redirect to `accounts.google.com` → confirmed it now renders the Google sign-in page instead of `Error 400: redirect_uri_mismatch`.
2. **Cloud drafting routine is blocked and disabled.** Created (`trig_01NoDrHQv29wBh3ssQQRZ2to`) but its cloud sandbox's network egress proxy blocks `modinekiyakyahai.com` entirely (org policy allowlist — only Anthropic's own API + npm/PyPI registries by default). User tried adding the domain via claude.ai portal capabilities/network-egress settings twice; identical failure both times (byte-identical proxy allowlist response), so whatever was changed isn't reaching this routine's environment (`env_01LGzWqzQV4cPUXF4hnadmfe`). **Routine is currently disabled** to avoid silent daily failures. Re-enable via `RemoteTrigger action:update, body:{enabled:true}` once the egress issue is actually confirmed fixed (retest with `action:run` + `get_run_log` before trusting it). The editorial pipeline built 2026-09-15 (`/api/pipeline/editorial-ingest`) has no scheduled runner at all yet for the exact same reason — same fix unblocks both. **Confirmed still broken as of 2026-09-15 08:10am IST** — the routine's own scheduled run that morning failed the exact same way (`EGRESS_BLOCKED` from both `curl` and `WebFetch`), so it's a real, persistent block, not a one-off. **Superseded as the primary automation path by GitHub Actions — see item 2a below.**
2a. **GitHub Actions drafting agents — the new durable automation path** (2026-09-15): since the Claude Code cloud routine's environment network policy is out of our control and has failed 3 times now, built a parallel path that doesn't depend on it. `scripts/pipeline/run-agent.mjs` — a plain Node script (no new dependency; raw `fetch` to the Anthropic Messages API) that runs an agentic loop with the server-side `web_search_20250305` tool plus custom tools wrapping this site's own pipeline HTTP API (`list_existing_entries`, `list_trusted_sources`, `list_editorial_candidates`, `submit_entry`, `submit_editorial`). Two scheduled workflows call it: `.github/workflows/draft-entries.yml` (3:37 UTC daily) and `draft-editorials.yml` (3:45 UTC daily), both also `workflow_dispatch`-triggerable for manual test runs. Runs on GitHub's own runners — open internet, no proxy allowlist to fight. The script reads `scripts/entries/OFFICIAL_SOURCES.md`, `MICRO_MACRO_LENS.md`, and (editorials mode) `EDITORIAL_VOICE.md` straight from the checked-out repo at run time and inlines them into the system prompt, so future edits to those docs apply to the very next scheduled run automatically — no prompt to keep in sync by hand, unlike the cloud routine's baked-in prompt. Everything it submits lands as `pending_review`, same as every other path. **Needs 2 GitHub repo secrets before it can run**: `ANTHROPIC_API_KEY` (new — user must generate one at console.anthropic.com; this is separate billing from the Claude Code subscription, pay-per-token + $10/1000 web searches) and `CRON_SECRET` (reuse the exact value already in Vercel/`.env.local`). Add both at github.com/authenticTyagi/namo → Settings → Secrets and variables → Actions → New repository secret. **Not yet end-to-end tested** — `.env.local`'s own `ANTHROPIC_API_KEY` line is present but empty, so no local smoke test has run either. Once the secret is added, trigger a manual run via the Actions tab's "Run workflow" button (or `workflow_dispatch`) and check the run log before trusting the daily schedule.
3. **Local cron bridges active as a stopgap**: job `a102b420` (entries drafting, daily ~9:07am IST, created 2026-09-14; already produced 2 pending entries — PLI scheme, INS Vikrant — see below) and job `e0cca1ce` (editorial drafting, daily 9:15am IST — recreated twice: once to add the official-sources reference, once to wire in `EDITORIAL_VOICE.md` after the voice negotiation; earlier IDs `64917ebb`/`4067e9b7` are dead). Both are SESSION-ONLY (die if this Claude Code session/terminal closes) and auto-expire after 7 days regardless. Not a real long-term solution — don't assume either is still running in a future session without checking `CronList`.
4. **`pipelineConfig`-driven auto-publish threshold**: intentionally unwired. Every pipeline-drafted entry lands as `pending_review` regardless of confidence score, until there's real calibration data from actual runs.
5. **Freshness re-verification of existing entries**: not built. Same pattern as the drafting pipeline would apply (a scheduled check on `lastVerifiedDate` age), just not done yet.
6. **AdSense**: consent banner + verification meta tag shipped (publisher ID `ca-pub-6506096146746148`, 2026-09-15). Actual ad units/slots not built yet — waiting on account approval.
7. **GitHub Actions/CI**: none set up; relying on Vercel's own build-time type-check+lint as the only gate.

## Explicitly out of scope so far (deferred, not forgotten)

- ~~Public comments on entries~~ — done 2026-09-15, live on both entries and editorials.
- Rich in-admin content editor (new/edited entries still go through `scripts/entries/*.ts` content-pack files + `npm run db:seed`, not a web form)
- `/admin/pipeline` page showing `pipelineRuns` history (visible via `npm run db:studio` for now)
- **Hand-authored Bengali/Telugu/Marathi content for the 22 existing entries** — the architecture landed 2026-09-15 (`entryTranslations`/`entryStatTranslations` tables, empty) but writing the actual translated content is a separate future pass. Until then every bn/te/mr entry page correctly falls back to English. Same content-authoring discipline should apply when this happens — no machine translation for factual claims, cross-check numbers, etc.
- Translating the legal/policy static pages (About/Methodology/Terms/Privacy) into bn/te/mr — needs owner review, not auto-generated.

## Official social media handles as sources (2026-09-15)

`scripts/entries/OFFICIAL_SOURCES.md` — a hand-verified "safe list" of Government of India social media handles (PMO, PIB, ISRO, RBI, MEA, MoD, NITI Aayog, MoHFW so far) that may be cited as `official_primary` sources when a post from that exact handle is the evidence for a claim. Wired into the local editorial cron bridge's prompt already. **Not yet wired into**: the local entries-drafting cron job (`a102b420` — its exact original prompt text isn't available this session to safely edit without risk of dropping detail; update it next time it's recreated) or the cloud routine's stored prompt (pointless while the routine can't reach the domain at all — and the cloud sandbox has no repo mounted, so it would need the list inlined into the prompt text rather than referenced by path; do that as part of actually re-enabling the routine, not before).

## Content authoring pattern (for adding new categories/entries)

One file per category under `scripts/entries/` (e.g. `phase1.ts`, `space.ts`, `foreign-policy.ts`, `defense.ts`, `currency-prices.ts`), each exporting `category` + an entries array, sharing types from `scripts/entries/types.ts`. Every entry: `quickTakeHi/En` (one-line hook) + `bodySectionsHi/En` (2-4 `{heading, body}` sections, NOT a single paragraph) + `stats[]` (before/after comparisons, `2014-or-launch` baseline vs now — research the real pre-2014 figure, don't guess) + `sources[]` (real URLs, tiered `official_primary`/`reputable_media`/`secondary`) + `tags[]`. Add the new pack to the `contentPacks` array in `scripts/seed.ts`. Add an icon per entry in `src/lib/entry-icons.ts` (verify the lucide-react icon name actually exists first — `node -e "console.log(typeof require('lucide-react').IconName)"`). Run `npm run db:seed` then deploy.

Research pattern that's worked well: WebSearch for current figures, cross-check across ≥2 independent sources, explicitly search for the pre-2014 baseline separately (don't derive/estimate it silently), flag any figure that varies across sources rather than picking the most flattering one.

## Categories not yet covered (candidates for next expansion)

**New-category candidates**: Women & Child Development (Beti Bachao Beti Padhao, Mission Shakti, women's workforce participation — distinct from the general household schemes already in Welfare), Naxalism/LWE (narrow security-statistics framing only — see the "declined topics" note above).

**New entries for EXISTING categories** (user-suggested 2026-09-15, mapped to categories rather than new ones): FDI inflows, IT/semiconductor exports, tax-payer base growth, per-capita income, BRICS, E20 ethanol blending, COVID-19 (vaccination scale + the honest 2020 GDP-contraction-then-recovery story), global-recession resilience → mostly `economy-infra-digital`/`currency-prices-inflation`/`foreign-policy-diplomacy`/`environment-renewable-energy`. EV adoption specifically (FAME scheme) also still open — touched on tangentially in Environment but no entry of its own yet.

**New comparison candidates**: per-capita income vs. peers, remittances/diaspora vs. other countries, tax base vs. peers, COVID recovery trajectory vs. peers.

Pick based on: (a) strong official/primary (or, for comparisons, neutral-international-body) sourcing available, (b) not yet duplicating an existing entry's topic (check `getAllEntrySlugsForDedupe` output or the 10 existing category pages).

## Session infra notes

- `npx vercel@latest` works but is NOT on PATH directly — always prefix with `npx --yes vercel`. Frequent harmless `Error: Failed to get package info: Error: Failed to fetch dist-tags from npm` noise on every invocation — ignore it, it doesn't affect the actual command.
- `npx auth secret` hangs (interactive prompt issue) — generate secrets via `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"` instead.
- Neon CLI (`npx neon@latest`) works; `neonctl`'s OAuth flow has a strict 60s window, retry fast if it times out.
- All secrets live in `.env.local` (gitignored) and Vercel's Production env vars — both need updating when adding a new one (`vercel env add NAME production`).
